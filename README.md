# Comments

Inline document comments with %%comment%% syntax for agent responses

## Structure

```
Shards/(Dev) Comments/
  shard.yaml              # Manifest
  init-cmts.md    # Init file — shard context
  skills/                 # sk-cmts-{name}.md
  workflows/              # wkfl-cmts-{name}.md
  templates/              # tmp-cmts-{name}.md
  knowledge/              # knw-cmts-{name}.md
  assets/                 # ast-cmts-{name}.{ext}
  install/                # Files to install into Mesh/
```
